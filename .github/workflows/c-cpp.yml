name: C/C++ CI

on:
  workflow_dispatch: 

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - uses: actions/upload-artifact@v3.2.2
      with:
        path: .
